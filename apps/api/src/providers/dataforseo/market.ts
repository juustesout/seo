/**
 * DataForSEO market target. Kept as one definition because these values change
 * what the vendor returns and therefore belong in a source snapshot's canonical
 * scope - the scope must never claim a snapshot is reusable across markets.
 */

/** United States location code. */
export const DATAFORSEO_LOCATION_CODE = 2840;

/** English language code. */
export const DATAFORSEO_LANGUAGE_CODE = 'en';
